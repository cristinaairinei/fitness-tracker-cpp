## Fitness Tracker – C++

University project implemented in C++.

The application manages fitness activities and includes:
- Layered architecture (domain, repository, controller, UI)
- CSV and JSON persistence
- Undo/redo functionality using the Command pattern
- Filtering using the Strategy pattern
- Graphical user interface (Qt)


